name: Trigger Finance Netherlands Stage Build

on:
  push:
    branches: [ main ]
    paths: [ 'netherlands/**' ]
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - uses: peter-evans/repository-dispatch@v2
        with:
          token: ${{ secrets.REPO_TOKEN }}
          repository: Aspose/apireference.aspose.com
          event-type: build-finance-netherlands-stage
